<?php
// Laravel API routes disabled.
// The backend is now provided by the Spring Boot service (see java-backend/).
// Keep this file present (required by bootstrap/app.php routing include),
// but don't register any API routes to avoid conflicts with the Java API.
